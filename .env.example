# Database connection — use DATABASE_URL OR individual fields
DATABASE_URL=postgresql://user:password@localhost:5432/gero

DB_HOST=localhost
DB_PORT=5432
DB_NAME=gero
DB_USER=postgres
DB_PASS=postgres

# Session
SESSION_SECRET=change-me-in-production

# App
NODE_ENV=development
